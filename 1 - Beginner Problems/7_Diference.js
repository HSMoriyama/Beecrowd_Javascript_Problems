/* Diference
* Read four integer values named A, B, C and D. Calculate and print the difference 
* of product A and B by the product of C and D (A * B - C * D).

Input
* The input file contains 4 integer values.

Output
* Print DIFERENCA (DIFFERENCE in Portuguese) with all the capital letters, according 
* to the following example, with a blank space before and after the equal signal.
*/

/* Diferença
* Faça a leitura de 4 valores inteiros, A, B, C e D. Calcule e exiba a diferença 
* entre o produto de A e B com o produto de C e D (A * B - C * D).

*Entrada
* A entrada será 4 valores inteiros.

*Saída
* Imprima DIFERENÇA com letras maiusculas, de acordo com o seguinte exemplo 
* com espaço em branco antes e depois do sinal de igual.
*/

/*CODE AREA(ÁREA DE CÓDIGO)*/
var A = 2;
var B = 4;

var C = 3;
var D = 9;

var diferenca = (A * B) - (C * D);

console.log(`DIFERENÇA = ${diferenca}`);