/* Simple Sum
* Read two integer values, in this case, the variables A and B. After this, 
* calculate the sum between them and assign it to the variable SOMA. Write 
* the value of this variable.

*Input
* The input file contains 2 integer numbers.

*Output
* Print the message "SOMA" with all the capital letters, with a blank space 
* before and after the equal signal followed by the corresponding value to 
* the sum of A and B.
*/

/* Soma Simples 
* Realize a leitura de 2 valores do tipo int, nesse caso, as variáveis A e B.  
* Após isso calcule a soma entre eles e atribua o resultado a variável SOMA. Escreva
* o valor dessa variável.

*Entrada
* A entrada serão dois números do tipo int.

*Saída
* Imprima a mensagem "SOMA" em letras maiúsculas, com espaços em branco 
* antes e depois do simbolo de igual, e em seguida o resultado da soma de A e B.
*/

/*CODE AREA(ÁREA DE CÓDIGO)*/
var A = 5;
var B = 5;
var soma = A + B;

console.log(`SOMA = ${soma}`);