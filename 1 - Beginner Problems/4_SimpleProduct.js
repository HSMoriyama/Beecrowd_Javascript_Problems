/* Simple Product
* Read two integer values. After this, calculate the product between them and 
* store the result in a variable named PROD. 

*Input
* The input file contains 2 integer numbers.

*Output
* Print the message "PROD", with a blank space before and after the equal signal.
*/

/* Multiplicação Simples
* Faça a leitura de 2 valores do tipo int. Após isso, calcule o produto entre eles e 
* atribua o valor do resultado a uma variável chamada PROD. 

*Entrada
* A entrada serão 2 números do tipo int.

*Saída
* Imprima a mensagem "PROD", com um espaço em branco antes e depois do sinal de igual.
*/

/*CODE AREA(ÁREA DE CÓDIGO)*/
var A = 5;
var B = 5;
var PROD = A * B;

console.log(`PROD = ${PROD}`);